import { ScrollView, StyleSheet } from 'react-native';

export default function TableBody() {
  return (
    <ScrollView style={styles.container}>{/* <TableRows /> */}</ScrollView>
  );
}

const styles = StyleSheet.create({
  container: {},
});
