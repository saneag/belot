import { DEFAULT_ROUND_POINTS, RoundScore } from '@belot/shared';

export const defaultRoundScore: RoundScore = {
  id: 0,
  playersScores: [],
  teamsScores: [],
  totalRoundScore: DEFAULT_ROUND_POINTS,
};
